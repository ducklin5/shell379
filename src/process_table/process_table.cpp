#include <iostream>

#include "process_table.h"

using std::cout;
int ProcessTable::addProcess(int pid, std::string command){
	if(table.count(pid) > 1){
		cout 
	}
}


int ProcessTable::killProcess(int pid);
